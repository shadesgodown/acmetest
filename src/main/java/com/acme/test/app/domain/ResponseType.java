package com.acme.test.app.domain;

/**
 * Enumeration of the response types available in the service.
 */
public enum ResponseType {
    HELLO_WORLD,
    FIBONACCI,
    DEADLOCK,
    CRUD,
    EXTERNAL_ENDPOINT;
}
